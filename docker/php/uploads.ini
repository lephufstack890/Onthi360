; Tăng giới hạn upload mặc định của PHP (mặc định upload_max_filesize=2M,
; post_max_size=8M) — không đủ cho ảnh bìa sản phẩm (validate ở
; ProductController cho phép tới 4MB/ảnh). Xem admin.products.create/edit.
upload_max_filesize = 10M
post_max_size = 12M
